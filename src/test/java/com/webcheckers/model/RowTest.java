package com.webcheckers.model;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
/**
 * Unit test for Row
 * @author Evan Price
 */

@Tag ("Model-tier")
class RowTest {

    /**
     * Test the constructor
     */
    @Test
    void ctor_withArg() {
        /*
        //test row 0
        final Row CuT0 = new Row(0, Piece.COLOR.WHITE);
        assertTrue(CuT0.getSpaceAtIndex(0).getBoardColor() == Space.BOARD_COLOR.WHITE);
        assertEquals("The piece on this space is null, the index of this space is 0 and, the color of this space is WHITE", CuT0.getSpaceAtIndex(0).toString());
        assertNull(CuT0.getSpaceAtIndex(0).getPiece());
        assertNotNull(CuT0.getSpaceAtIndex(0));
        //test row 1
        final Row CuT1 = new Row(1, Piece.COLOR.WHITE);
        assertTrue(CuT1.getSpaceAtIndex(1).getBoardColor() == Space.BOARD_COLOR.BLACK);
        assertEquals("The piece on this space is null, the index of this space is 1 and, the color of this space is BLACK", CuT1.getSpaceAtIndex(1).toString());
        assertEquals(Piece.TYPE.SINGLE, CuT1.getSpaceAtIndex(1).getPiece().getType());
        assertNotNull(CuT1.getSpaceAtIndex(1));
         */
    }
}